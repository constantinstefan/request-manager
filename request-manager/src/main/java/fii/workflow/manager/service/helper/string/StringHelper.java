package fii.workflow.manager.service.helper.string;

public class StringHelper {
    public static String toUpperCase(String string) {
        return (string != null)
                ? string.toUpperCase()
                : null;
    }
}
